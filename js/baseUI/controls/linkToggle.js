define(['react', 'lodash', 'baseUI/controls/linkToggle.rt'], function (React, _, template) {
    'use strict';

    return React.createClass({
        displayName: 'linkToggle',
        render: template
    });
});
