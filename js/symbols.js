var symbol = require('symbols/control/symbol');
var symbolsData = require('symbols/data/symbols');

require('symbols/symbols.scss');

module.exports = {
    symbol: symbol,
    symbolsData: symbolsData
};