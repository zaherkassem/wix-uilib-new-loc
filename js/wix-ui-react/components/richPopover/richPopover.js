var template = require('wix-ui-react/components/richPopover/richPopover.rt');
//require('baseUI/richPopover/richPopover.scss');

module.exports = React.createClass({
    displayName: 'richPopover',
    render: template
});
