define(['react', 'lodash', 'textControls/comps/textEffectButton.rt'], function (React, _, template) {
    'use strict';

    return React.createClass({
        displayName: 'textEffectButton',
        render: template
    });
});
